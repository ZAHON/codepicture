'use client';
import { DialogHeader, DialogTitle, DialogClose } from '@/components';

export function KeyboardShortcutsDialogHeader() {
  return (
    <DialogHeader>
      <DialogTitle>Keyboard shortcuts</DialogTitle>
      <DialogClose />
    </DialogHeader>
  );
}
