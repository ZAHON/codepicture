'use client';
import { DialogHeader, DialogTitle, DialogClose } from '@/components';

export function ExportImageDialogHeader() {
  return (
    <DialogHeader>
      <DialogTitle>Export image</DialogTitle>
      <DialogClose />
    </DialogHeader>
  );
}
