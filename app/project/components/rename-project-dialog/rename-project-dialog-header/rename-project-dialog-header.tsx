'use client';
import { DialogHeader, DialogTitle, DialogClose } from '@/components';

export function RenameProjectDialogHeader() {
  return (
    <DialogHeader>
      <DialogTitle>Rename project</DialogTitle>
      <DialogClose />
    </DialogHeader>
  );
}
